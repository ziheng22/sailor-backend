services:
  api:
    build: .
    ports:
      - "${API_PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      - ./sailor.db:/app/sailor.db
      - ./uploads:/app/uploads
    restart: unless-stopped
